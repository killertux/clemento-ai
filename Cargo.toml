[package]
name = "clemento-ai"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.8"
rayon = "1"
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
lto = "thin"
